const product = 'Socks'
const description = 'Socks description'

const app = Vue.createApp({
    data() {
        return {
            product: product,
            description: description
        }
    }
})
